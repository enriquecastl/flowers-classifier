# Number of flower categories
categories_count = 102
# Default dropout
dropout = 0.2
# Default checkpoint filenames
checkpoint_filename = 'flowers_classifier.pth'
# Image size as expected by the torchvision models
img_size = 244
# Image channels normalization means as expected by the torchvision models
norm_mean = [0.485, 0.456, 0.406]
# Image channels normalization STD as expected by the torchvision models
norm_std = [0.229, 0.224, 0.225]